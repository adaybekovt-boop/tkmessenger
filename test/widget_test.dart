// Placeholder widget test. The scaffolded `MyApp` counter example was
// removed when the real `main.dart` landed — this file stays so
// `flutter test` doesn't complain about a missing entry-point.
//
// Real widget coverage lives alongside features (e.g. `test/ui/chat/`).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
